{
    // Add paths for Python to recognize the modules in these directories
    "python.analysis.extraPaths": [
        "${workspaceFolder}/config",
        "${workspaceFolder}/src",
        "${workspaceFolder}/src/address_book",
        "${workspaceFolder}/src/contacts",
        "${workspaceFolder}/src/decorators",
        "${workspaceFolder}/src/handlers",
        "${workspaceFolder}/src/utils",
        "${workspaceFolder}/src/validators",
    ]
}
